formulas:
  Untitled: |-
    LIST
    FROM "World/NPC"
    WHERE !contains(text, "🔴 Dead")
views:
  - type: table
    name: Table
    filters:
      and:
        - file.hasLink("World/Towns/Ambervale")
        - file.folder == "Family/Members"
    order:
      - file.name
